#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;

#define rep(i, a, n) for (int i = a; i < n; ++i)
#define per(i, a, n) for (int i = n - 1; i >= a; --i)
#define fi first
#define se second

#ifndef int
#define int ll
int exgcd(int a, int b, int &x, int &y)
{
    // Your code.
    return d;
}

int qpod(ll x, ll a, ll mod)
{
    // Your code.
    return res;
}

#endif

int main()
{
    ios::sync_with_stdio(false);
    // cout << n << " " << p << " " << q << " " << e << endl;
    // Your code
    // exgcd(e, l, x, y);
    // Your code
    // cout << qpod(c, d, n);

    return 0;
}